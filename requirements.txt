# Universal CAN-Bus Diagnostic & Telemetry Platform
# Core & Desktop Engine Dependencies
# NOTE: upper bounds below are supply-chain pins ("# pinned upper bound (supply-chain)").
# Reproducible hash-locked requirements are committed in requirements.lock (T62-U6).
# For verified reproducible installations: pip install --require-hashes -r requirements.lock.

# CAN Hardware & Protocol Stack
python-can>=4.4.0,<5  # pinned upper bound (supply-chain)
can-isotp>=2.0.2,<3  # pinned upper bound (supply-chain)
cantools>=39.0.0,<41  # pinned upper bound (supply-chain)

# Telemetry & Math Processing
# CI NOTE (windows-latest, python 3.13): numpy only publishes cp313 win_amd64
# wheels from 2.1.0 onward. Any pin whose ceiling is <2.1 (e.g. <2, <2.1) has
# NO 3.13 wheel, so pip falls back to the sdist, PEP-517 builds it with a
# MinGW-w64 toolchain, and the resulting extension dies on first import with
# 'invalid value encountered in exp2/nextafter' (broken float128 limits) —
# which surfaced as a crash in the asammdf wheel-check step.
# numpy 2.1.0 is therefore the FLOOR for 3.13, not the ceiling.
numpy>=2.1.0,<3  # 2.1.0 = first release with cp313 wheels; SciPy>=1.14 supports numpy 2.x
scipy>=1.14.0,<2  # 1.14+ is the first SciPy line that builds against numpy 2.x
asammdf>=8.1.0,<9  # 8.1+ = cp310-abi3 wheels (3.10-3.13 binary, no sdist build)
simplekml>=1.3.6,<2  # pinned upper bound (supply-chain)
pandas>=2.2.0,<3  # pinned upper bound (supply-chain)
zstandard>=0.22.0,<1  # pinned upper bound (supply-chain)

# Desktop GUI & Qt Framework
pywebview>=5.0.0,<7  # pinned upper bound (supply-chain)

# Packaging & Standalone Executable
pyinstaller>=6.0.0,<7  # pinned upper bound (supply-chain)

# Security, Cryptography & Licensing
cryptography>=42.0.0,<52  # pinned upper bound (supply-chain)
pydantic>=2.6.0,<3  # pinned upper bound (supply-chain)
pydantic-settings>=2.2.0,<3  # pinned upper bound (supply-chain)

# Structured Logging & Utilities
structlog>=24.1.0,<26  # pinned upper bound (supply-chain)
python-dotenv>=1.0.0,<2  # pinned upper bound (supply-chain)
